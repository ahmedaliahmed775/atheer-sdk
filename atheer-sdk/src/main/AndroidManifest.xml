<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atheer.sdk">

    <!-- تصاريح NFC الضرورية للمدفوعات اللاتلامسية -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="true" />

    <!-- تصاريح الشبكة الخلوية الإجبارية لـ Atheer SDK -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application>
        <!--
            خدمة HCE الرئيسية - محاكاة البطاقة المضيفة
            تم ضبطها لتكون خدمة دفع (Payment Service) رسمية
        -->
        <service
            android:name=".hce.AtheerApduService"
            android:exported="true"
            android:label="@string/atheer_payment_service_label"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>

        <!--
            إضافة نشاط (Activity) للإعدادات أو للمعلومات - اختياري لكنه مفضل
            ليتمكن المستخدم من الوصول لإعدادات الدفع الخاصة بـ Atheer
        -->
        <activity
            android:name=".AtheerPaymentSettingsActivity"
            android:exported="true"
            android:label="إعدادات دفع Atheer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.PAYMENT_SETTINGS" />
            </intent-filter>
        </activity>

    </application>
</manifest>
